services:
  warehouse-flow-manager-frontend:
    image: punschkrapferl23/warehouse-flow-manager-frontend:1.0.0
    build:
      context: .
      dockerfile: Dockerfile
    container_name: warehouse-flow-manager-frontend
    ports:
      - "4200:80"
    restart: unless-stopped
